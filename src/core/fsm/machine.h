#pragma once

/*typedef enum
{
    EVENT_OK,
    EVENT_INVALID_STATE,
    EVENT_INVALID_ID,
    EVENT_BUSY,
    EVENT_UNKNOWN_ERROR

} EventResult;*/

void machine_init(void);
void machine_update(void);